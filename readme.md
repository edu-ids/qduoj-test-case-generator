# QDUOJ Test Case Writer
## How To
1. Create test cases:
  * Input: in/${number}.in
  * Output: out/${number}.out
2. Run `gen.py`
3. Upload output.zip

## 使い方
1. テストケースを作成
  * Input: in/${number}.in
  * Output: out/${number}.out
2. Run `gen.py`
3. Upload output.zip